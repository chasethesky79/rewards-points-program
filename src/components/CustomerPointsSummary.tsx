import { ICustomerPointsSummaryWrapper } from "../models/rewards-models";

export const CustomerPointsSummary: React.FC<ICustomerPointsSummaryWrapper> = ({ customerPointsSummary }: ICustomerPointsSummaryWrapper) => {
    return (
        <div></div>
    )
}